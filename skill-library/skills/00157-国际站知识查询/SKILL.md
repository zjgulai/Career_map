---
name: 国际站知识查询
version: "1.0.0"
description: |
  检索国际站官方 FAQ/知识库，回答平台经营规则、操作指引、服务政策和后台使用问题。
  覆盖入驻、建站发品、营销商机、数据、生意助手、信保订单、资金/发票、物流、一达通、Alibaba.com Pay、金融、安全网规/禁售、买家服务、半托管、无忧退等知识场景；不执行具体业务操作或实时数据查询。
enabled: true

triggers:
  - 平台规则
  - 操作指引
  - 官方 FAQ
  - 服务政策
  - 后台怎么操作
  - 信用保障订单规则
  - 资金发票
  - 安全网规
  - 禁售规则
  - i 豆扣费
  - 账号注销
  - 审核时效

examples:
  - 信用保障订单怎么取消？
  - 国际站账号注销流程是什么？
  - i 豆为什么被扣费？
  - 商品审核一般需要多久？
  - 无忧退的规则是什么？

excludes:
  - skill: alibaba-product-publish
    when: 用户要实际发品、解析素材、发布草稿或查询发品任务结果
  - skill: alibaba-product-optimization
    when: 用户要实际编辑、修改或优化商品字段
  - skill: alibaba-logistics-assistant
    when: 用户要实时查运费、物流线路、物流订单、发货、HS 编码或关税试算
  - skill: alibaba-analysis-brief
    when: 用户要查询或分析自己店铺经营数据
  - skill: alibaba-chat-and-analysis
    when: 用户要查询聊天记录、筛选买家、分析询盘、生成回复话术，或在明确确认后发送买家消息
---

## Workctl 服务配置

### 服务信息

|     |   |
|--------|----|
| **服务名称：** |  国际站知识查询 |
| **调用工具：** |  `workctl` |
| **完整命令：** |  `workctl icbu cco icbu-faq-knowledge-chunk` |

> 执行时必须原样使用上述完整命令，不得省略、改写或重新拼装任何命令层级。

### 调用参数
```json
{
  "query": "什么是信用保障订单"
}
```

### 调用方法
```bash
workctl icbu cco icbu-faq-knowledge-chunk \
  --query "什么是信用保障订单" \
  --format json
```

## 错误处理与降级交付

当上述完整命令调用失败、返回为空或权限受限时，**禁止只说"无法完成"**，必须按以下降级路径输出可用产物：

- **接口调用失败：** 自动重试一次（不同时刻可能恢复）。仍失败时：
  - 告知用户具体错误（错误码 + 接口名）。
  - 基于自身知识就 query 主题给出**通用回答框架**（标注"⚠️ 知识库接口不可用，以下为通用说明，建议刷新后从知识库验证"）。
  - 严禁用"无法回答"或"请稍后再试"作为最终交付。

- **接口返回为空（query 在知识库中无匹配 QA）：**
  - 不要直接抛"未找到知识"。先做一次 query 同义改写（如把"信保订单"改写为"信用保障订单"、"信任订单"等），再调用一次。
  - 仍为空时，明确告知"该问题不在国际站官方 FAQ 知识库中"，并主动给出 2-3 个相近主题的官方文档链接，让用户自行选择。

- **能力边界前置告知：** 当用户问题涉及实时操作（如"帮我修改运费模板"）、跨平台能力（如 Lazada / 速卖通）或需要订单/账号上下文时，本 skill 不应承担解答职责：
  - 立即告知"本 skill 只支持国际站官方 FAQ 知识库的检索回答"。
  - 引导用户使用对应的操作类 skill，禁止编造 FAQ 内容。

- **回答完整性兜底：** 凡基于上述完整命令返回内容回答时，必须保留至少 1 条「相关 QA 引用」（Q + A 摘要 + 出处链接），便于用户溯源；接口未返回出处字段时，至少标注"来源：国际站官方 FAQ 知识库"，禁止把回答伪装为模型自身知识。

- **规则仍然模糊时：** 无法从官方知识片段唯一确认口径时，只给保守建议，并明确标注“建议到卖家中心确认”；不得把推测包装成确定的平台规则。

- **推理中止与熔断：**
  - **搜索尝试限制：** 若连续 3 次通过上述完整命令或同类搜索工具（如同义词改写后）均未获取到有效知识片断，必须立即中止自主推理，不得继续尝试其他搜索或扫描本地文件。
  - **无果处理：** 中止后进入 fallback 引导：告知用户"已深度检索国际站官方知识库，暂未发现与『{query}』直接相关的官方说明"，并提供官方客服链接或建议用户换个关键词提问。
  - **禁止事项：** 严禁在知识库无果时尝试 `ls`、`list`、`grep` 等文件系统工具来寻找云端知识库配置或文档。
