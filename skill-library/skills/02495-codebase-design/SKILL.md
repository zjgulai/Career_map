---
name: "codebase-design"
title: "代码库深模块设计"
description: "深模块设计词汇：设计与改进模块接口。触发词：代码库深模块设计、codebase-design、深模块设计词汇：设计与改进模块接口。"
enabled: "true"
disable-model-invocation: false
user-invocable: true
---

# Codebase Design

设计**深层模块（deep module）**：一个小接口背后装下大量行为，落在一个干净的接缝上，可以通过这个接口测试。在任何设计或重构代码的地方，都使用这套语言和这些原则。目标是为调用方提供杠杆（leverage）、为维护者提供局部性（locality）、为所有人提供可测试性。

## 术语表

精确使用这些术语：不要用「component（组件）」「service（服务）」「API」「boundary（边界）」来替代。语言一致正是全部意义所在。

**Module（模块）**：任何有接口和实现的东西。刻意与规模无关：可以是一个函数、一个类、一个包，或一个跨层的切片。_避免说_：unit（单元）、component（组件）、service（服务）。

**Interface（接口）**：调用方为正确使用该模块而必须知道的一切：类型签名，但也包括不变量、顺序约束、错误模式、必需的配置，以及性能特征。_避免说_：API、signature（签名，太窄了，它们只指类型层面的表面）。

**Implementation（实现）**：模块内部的东西，它的代码主体。与 **Adapter（适配器）**不同：一个东西可以是一个实现很大的小适配器（比如 Postgres 仓库），也可以是一个实现很小的大适配器（比如内存中的 fake）。当话题是接缝时，用「适配器」；否则用「实现」。

**Depth（深度）**：接口处的杠杆。调用方（或测试）每学习一单位接口，能调动多少行为。当一个模块在小接口背后装下大量行为时，它就是**深的**；当接口几乎和实现一样复杂时，它就是**浅的**。

**Seam（接缝）**（_Michael Feathers 提出_）：一个你可以在不编辑该处代码的情况下改变行为的地方；是模块接口所在的*位置*。把接缝放在哪里本身就是一项设计决策，与它背后装什么无关。_避免说_：boundary（与 DDD 的限界上下文含义重载）。

**Adapter（适配器）**：在接缝处满足一个接口的具体事物。描述的是*角色*（它填哪个槽位），而不是实质（它里面是什么）。

**Leverage（杠杆）**：调用方从深度中获得的东西。每学习一单位接口获得更多能力。一份实现，在 N 个调用点和 M 个测试上回本。

**Locality（局部性）**：维护者从深度中获得的东西。变更、缺陷、知识和验证集中在一个地方，而不是散布在调用方之间。修一次，处处修好。

## 深 vs 浅

**深层模块** = 小接口 + 大量实现：

```
┌─────────────────────┐
│   Small Interface   │  ← Few methods, simple params
├─────────────────────┤
│                     │
│  Deep Implementation│  ← Complex logic hidden
│                     │
└─────────────────────┘
```

**浅层模块** = 大接口 + 少量实现（要避免）：

```
┌─────────────────────────────────┐
│       Large Interface           │  ← Many methods, complex params
├─────────────────────────────────┤
│  Thin Implementation            │  ← Just passes through
└─────────────────────────────────┘
```

设计接口时，问自己：

- 我能减少方法的数量吗？
- 我能简化参数吗？
- 我能把更多复杂度藏到内部吗？

## 原则

- **深度是接口的属性，不是实现的属性。**一个深层模块内部可以由小的、可 mock 的、可替换的部件组成；它们只是不属于接口。一个模块既可以有**内部接缝**（对实现私有，供它自己的测试使用），也可以有位于接口处的**外部接缝**。
- **删除测试。**想象删掉这个模块。如果复杂度随之消失，那它就是个传声筒。如果复杂度在 N 个调用方那里重新冒出来，那它一直在挣自己的口粮。
- **接口就是测试面。**调用方和测试跨越同一个接缝。如果你想要测到接口*背后*去，那这个模块的形态可能就不对。
- **一个适配器意味着一个假设的接缝。两个适配器才意味着一个真实的接缝。**除非确实有东西在它两侧变化，否则不要引入接缝。

## 为可测试性而设计

好的接口让测试变得自然：

1. **接受依赖，而不是创建依赖。**

   ```typescript
   // Testable
   function processOrder(order, paymentGateway) {}

   // Hard to test
   function processOrder(order) {
     const gateway = new StripeGateway();
   }
   ```

2. **返回结果，而不是产生副作用。**

   ```typescript
   // Testable
   function calculateDiscount(cart): Discount {}

   // Hard to test
   function applyDiscount(cart): void {
     cart.total -= discount;
   }
   ```

3. **小的表面积。**方法越少 = 需要的测试越少。参数越少 = 测试搭建越简单。

## 关系

- 一个**模块**恰好有一个**接口**（它呈现给调用方和测试的那个面）。
- **深度**是**模块**的属性，对照它的**接口**来衡量。
- **接缝**是模块**接口**的所在之处。
- **适配器**坐在**接缝**处，满足**接口**。
- **深度**为调用方带来**杠杆**，为维护者带来**局部性**。

## 被否决的表述框架

- **把深度当作实现行数与接口行数之比**（Ousterhout）：这会奖励给实现注水。我们改用「深度即杠杆」。
- **把「接口」当作 TypeScript 的 `interface` 关键字或类的公共方法**：太窄了：这里的接口包括调用方必须知道的每一个事实。
- **「边界」**：与 DDD 的限界上下文含义重载。应该说**接缝**或**接口**。

## 进一步深入

- **给定依赖关系来深化一个簇**，见 [DEEPENING.md](DEEPENING.md)：依赖类别、接缝纪律，以及「替换而非分层」的测试。
- **探索替代接口**，见 [DESIGN-IT-TWICE.md](DESIGN-IT-TWICE.md)：启动并行的子代理，用几种截然不同的方式设计接口，然后在深度、局部性和接缝位置上比较。
