# Skill Archive

本目录按“编号-核心功能”命名；与 `skill_inventory_enriched.xlsx` / `skill_inventory_enriched.json` 的 `skill_id` 一一对应，完整对应关系见 `skill_archive_folder_index.csv` / `skill_archive_folder_index.json`。

- 总记录：12871
- 可读的标准目录型 `SKILL.md`：10137
- 可读 Markdown 文件型资产镜像为入口：7
- 文件型 JSONL 资产生成说明入口：1133

- 不透明 paper Markdown 资产生成说明入口：1349
- 不透明标准目录型入口生成说明入口：245

每个子目录均应包含 `SKILL.md` 和 `metadata.json`。对于 JSONL 文件型资产，`SKILL.md` 是检索和业务理解入口，同目录原始 JSONL 仍是来源归档；对于不透明 Markdown 也采用同一保留与说明方式。它们不自动等同于可安装或已通过竞聘的运行时 Skill。
