---
name: 创意工坊视频生成
version: "1.0.0"
description: |
  统一调度视频生成能力，包含商品场景视频和多分镜视频生成两大子能力，覆盖商品场景视频、图片生成视频、文生视频和多分镜视频。
  根据具体需求路由到对应子能力；不处理图片生成、视频后期剪辑或商品发布。
enabled: true

triggers:
  - 生成视频
  - 商品视频
  - 商品场景视频
  - 图片生成视频
  - 文生视频
  - 多分镜视频
  - 创意视频
  - 视频素材
  - 产品展示视频

examples:
  - 给这个产品做一个场景展示视频
  - 参考这张图生成一个多分镜视频
  - 根据这段文字生成一个商品宣传视频
  - 生成一个 6 秒的产品视频

excludes:
  - skill: image-prompt-guide
    when: 用户要生成或处理图片、白底图、场景图、去水印、换色、Logo、高清图或模特图
  - skill: alibaba-product-publish
    when: 用户要把视频/素材用于发品、发布商品或素材包发品
  - skill: alibaba-create-website
    when: 用户要创建、编辑或发布店铺装修网页
  - skill: alibaba-product-optimization
    when: 用户要修改商品详情、主图、视频字段并保存或发布
renderers:
  index:
    description: render index page
    url: https://g.alicdn.com/ife/aw-video-generate/1.0.2/index.html
---

## 执行约束

1. **不复用历史结果**：即使用户会话与之前会话相似，也必须重新执行完整流程，不得跳步或复用之前的解析/预测结果。
2. **禁止访问记忆/缓存**：本 skill 被激活时，严格禁止以下行为：
    - 禁止读取任何记忆文件: MEMORY.md、日记文件（diary files）、代理记忆（agent memory）或任何持久化内存存储文件。
    - 禁止读取 memory/queries.json、TASK_HISTORY.json、MERCHANT_PROFILE.json 等文件中与视频生成任务相关的历史数据。
    - 禁止使用任何记忆搜索工具（例如 `memory_search`、`search_memory`、`recall_memory`）

---

# 视频生成路由技能

本 Skill 统一调度视频生成相关能力，执行目标是根据用户需求自动路由到对应的子 Skill

## 能力边界

本技能**仅覆盖 2 种视频生成能力**：商品场景视频、多分镜视频生成。

**以下场景均不属于本视频生成能力范围：**

- ❌ **视频后期类**：视频剪辑 / 拼接 / 裁剪 / 后期处理
- ❌ **音频字幕类**：替换音频 / 加字幕 / 视频翻译 / 视频配音
- ❌ **图像生成类**：生图 / 修图 / 图片处理（去背景、换色、加 Logo、模特图等）

**意图识别注意事项：**

- 用户附带视频要求"分析/点评" → 属于视频审阅意图，不执行本技能。
- 用户要求"写视频脚本/文案/分镜文字" → 属于文案生成意图，不执行本技能。
- 用户意图模糊（既像生图又像生视频）→ 必须追问确认，禁止默认假设。

## 子能力路由

### 🎬 1. 商品场景视频 (scene_video_generate_skill)

**文件路径**：references/scene_video_generate_skill.md
**触发条件**（必须全部满足）：

- 用户明确要求生成商品/产品的场景视频
- 提供了一张图片输入 (非多张图片)
- 未提到"多分镜"
- 若提到时长，则 ≤ 8 秒

**示例请求**：

- "生成这个透明玻璃杯的视频，阳光透过透明杯身和金黄色液体在桌面上投射出焦散光影，光影缓慢自然移动，镜头进行极小范围的平移"（附带商品图片）
- "给这个产品做一个场景展示视频"（附带商品图片）
- "生成一个 6 秒的商品视频"（附带商品图片）

### 🎞️ 2. 多分镜视频生成 (command_video_generate_skill) — 默认路由

**文件路径**：references/command_video_generate_skill.md

**触发条件**：除明确命中「商品场景视频」之外的所有视频生成场景，均走此能力（无论是否提供图片输入）。典型包括：

- 文生视频（无图片输入）
- 用户提到"多分镜"
- 时长 > 8 秒

**示例请求**：

- "参考这张图，生成一个多分镜的视频"
- "帮我制作一个视频，分镜1:xxx, 分镜2:xxx..."
- "生成一个包含 4 个分镜的创意宣传视频"
- "根据这段文字生成一个视频"（文生视频）

## 路由判定逻辑

```
IF 用户明确要求"商品/产品场景视频"
   AND 提供了图片输入
   AND 未提及"多分镜"
   AND （未提及时长 OR 时长 ≤ 8 秒）
THEN → references/scene_video_generate_skill.md

ELSE → references/command_video_generate_skill.md（默认路由）
```

## 执行流程

路由确定后：

1. 读取对应子 skill 文档
2. 严格按照对应子 skill 的指引执行视频生成流程
3. **严格遵守子 skill 中定义的输出格式执行视频生成**

> 本 skill 仅负责路由决策。所有后续执行流程（输入校验、参数提取、命令调用、结果轮询、错误处理、输出格式等）均以路由到的子 skill 文档为准。

## 未命中处理

当用户请求无法明确判断属于本技能覆盖的视频生成场景时（包括越界请求与意图模糊请求），返回以下引导文案：

---

您好，我支持以下视频生成能力，请告诉我您需要哪种：

1. **商品场景视频** - 为商品生成场景展示视频
2. **多分镜视频生成** - 生成多分镜创意视频（适用于广告、宣传、产品介绍等）

请描述您的具体需求，例如：

- "帮我生成一个 6 秒的商品场景视频"
- "帮我做一个多分镜的商品视频"

---
