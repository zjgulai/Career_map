---
name: "p2s-helmet-longcontext-rag-evaluation"
title: "Skill-HELMET-LongContext-RAG-Evaluation"
description: "触发词：长上下文评测、RAG 评估、模型选型、理解边界、多维度评测。何时不用：嵌入模型选型用「MTEB 嵌入模型选型」；分类或预测模型的指标与阈值评估用「Model Evaluation Metrics」。安全边界：卡页未给出评测数据集与合规要求，评测结论须标注上下文长度与样本范围，不得外推到未覆盖的任务。"
l1_id: "PLN-OPS"
l1_plane: "业务运营"
l2_id: "DOM-02"
l2_domain: "产品与创新"
l3_id: "DOM-02-036"
l3_business: "算法评估设计"
l3_all: "算法评估设计 / 业务工具实现"
l1_l2_l3: "业务运营/产品与创新/算法评估设计"
p2s_card_id: "Skill-HELMET-LongContext-RAG-Evaluation"
p2s_src_domain: "08-知识图谱"
quality_tier: "preview"
user_summary: "用 HELMET 这类多维度长上下文评测，在 128K 上下文的模型之间比出谁真正能用，避免上线后才发现理解边界不够。"
user_try: "试试：用 HELMET 的多维度评测对比这几个长上下文模型，选出最适合我们 RAG 场景的那个。"
whenToUse: "要在多个长上下文 LLM 之间做选型、需要知道各自真实理解边界时用本技能；若选型对象是嵌入模型，用「MTEB 嵌入模型选型」；若评估的是分类或预测模型的指标与阈值，用「Model Evaluation Metrics」。"
workflow: "准备候选长上下文模型与覆盖目标长度的评测样本 → 按 HELMET 的多维度任务集执行评测 → 汇总各模型在不同维度与上下文长度上的得分 → 输出理解边界结论与选型建议"
enabled: "true"
disable-model-invocation: "true"
user-invocable: "true"
---

# Skill-HELMET-LongContext-RAG-Evaluation

## ① 解决的问题

技术团队面临128K上下文LLM选型标准缺失——HELMET 20+维度评测识别真正理解边界，选型准确避免上线后翻车，年化决策价值35万元

## ② 核心算法逻辑

SkillHELMETLongContextRAGEvaluation

## ③ 业务应用场景

（卡页此段是占位串，未承载业务场景；可读的落地口径见下方「执行步骤」与「输入 / 输出契约」。）

## ④ 输入数据要求

（卡页此段是占位串，实际输入规格见下方「输入 / 输出契约」。）

## ⑤ 输出结果

（卡页此段是占位串，实际输出规格见下方「输入 / 输出契约」。）

## ⑥ 业务价值 / ROI

（卡页此段是占位串，本卡未记录 ROI。）

## ⑦ 代码节选

（卡页此段未附代码。但语料 vault 的同一张卡里有代码：本技能已附 `references/implementation.py`（202 行）。⚠️ 本卡卡面无节选可作对照，该文件取的是最长代码围栏，**未经交叉核对**。）

## ⑧ 论文来源

**出处待人工判定**：卡页写的是 arXiv:2410.02694。

本卡可用的英文标题词只有 1 个，机器判据给不出可信结论，故**不做断言**。需要引用时请人工看一眼这篇论文是否对口。

## 输入 / 输出契约

**输入**：待评测的长上下文模型清单与评测样本（需覆盖目标上下文长度）；卡页第 3/4 段未自动抽取，评测数据集与维度配置需回看原始卡页确认。

**输出**：各模型在多维度评测上的得分与可用上下文边界结论，作为选型依据（卡页称年化决策价值 35 万元）；卡页第 5 段未自动抽取，交付形态需回看原始卡页确认。

## 执行步骤

1. 准备候选模型与覆盖目标长度的评测样本
2. 按 HELMET 多维度任务集执行评测
3. 汇总各维度与上下文长度的得分
4. 输出理解边界结论与选型建议

## 边界与不做

- 卡页第 3/4/5/7 段未自动抽取，缺少明确评测数据集与场景定义时不得直接投产，需先回看原始卡页
- 评测结论只对所用任务集与上下文长度成立，不能外推到未覆盖的任务类型；输出是选型依据，不含成本与服务稳定性判断
- 卡页未给出数据处理与合规要求，涉及业务语料评测时须先脱敏

## 技能关联

- **可组合**：Skill-HELMET-LongContext-RAG-Evaluation

---

> 分类：业务运营/产品与创新/算法评估设计　·　技术族：08-知识图谱　·　源卡：`Skill-HELMET-LongContext-RAG-Evaluation`